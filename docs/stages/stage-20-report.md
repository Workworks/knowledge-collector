# Stage 20 阶段报告：相似资料与事件聚合

## 完成内容

- 新增事件、聚类键、事件日期和跟踪状态。
- 同一事件可关联多篇文章，记录官方、媒体等来源角色以及确认状态。
- 工作台按事件展示已聚合文章数量。

## 边界

本阶段提供人工聚合与自动聚类的数据接口；向量相似度和自动事件候选保留给 `EmbeddingProvider` 与 `ContentClusteringProvider`。
