# 测试报告

## 执行信息

- 日期：2026-07-18
- Java：17.0.8
- 命令：`./mvnw clean verify`
- 测试套件：21
- 测试：34
- 失败：0
- 错误：0
- 跳过：0

## 重点结果

- Flyway V1—V14 从空数据库迁移成功。
- RSS、Atom、HTML、去重、主题、质量、阅读管理和运维回归通过。
- RSS 文章详情页正文提取、安全清洗和已有空正文文章回填通过。
- Windows Surefire XML 中中文超时提示保持正常，不再出现重复解码乱码。
- 2.5 秒慢响应在来源 1 秒超时后转为 `HTTP-REQUEST-TIMEOUT`。
- 超时任务结束后，同一采集员可以立即创建下一任务。
- 20 分钟无心跳任务被回收为 `TASK-TIMEOUT`，来源锁正常释放。
- 失败重试、待执行取消和调度触发通过。
- Windows 系统根证书库和附加 PEM CA 加载通过，未关闭证书校验。
- 备份 ZIP 成功创建；H2 在线备份被恢复到新目录，恢复库可查询文章表。
- WireMock 模拟 Ollama 状态与结构化生成接口，AI 摘要、要点、关键词、标签、分类、评分和持久化通过。
- AI Provider 状态接口、文章 AI 接口、文章详情页和运维状态面板通过。
- AI 多轮会话、历史持久化和回复保存资料库通过。
- AI 回复以 `AI_GENERATED`、`PENDING_REVIEW` 入库并显示 AI 内容标记。
- `MANUAL_URL` Provider 注册及单页正文提取通过。
- 文章 AI 分析结果在 1280px 和 900px 视口下均无横向溢出。
- AI 采集源候选去重、访问验证和 `VERIFIED` 入库通过。
- 采集源健康刷新、最近七天任务过滤、归档资料及整理规则通过。
- AI 完整结构化分析和自动推荐待确认知识卡片通过。
- 卡片关系、观点证据、实体合并、事件、专题、研究、综合、写作、缺口与复习链路通过。
- 知识工作台桌面和 375px 窄屏浏览器验收通过，无横向溢出或控制台错误。
- 主题、资料库、知识工作台、AI 助手、网页提取和文件快照等页面统一显示固定十二项导航，切换页面时仅活动项变化。
- Ollama 与 SearXNG 统一配置、连接测试、模型列表、启停、默认 Provider 和调用日志通过。
- SearXNG 搜索候选、可靠性结果、单个/批量验证和导入正式采集源通过。
- Firecrawl 与 Playwright 模拟服务确实收到抓取请求；标题、作者、正文、耗时、截图和失败重试链持久化通过。
- 指定文章 ID 的成功提取会更新文章正文；手动 URL 不指定文章时只保留独立提取记录。
- MinIO Provider 的原始 HTML、截图、补充材料保存与下载通过；相同归属和文件类型连续上传形成 v1/v2。
- “网页提取”“文件与快照”桌面浏览器和 375px 窄屏验收通过，无页面级横向溢出或控制台错误，导航活动项唯一。
- 收藏与默认 AI 分析完成后，悬浮提示不参与阅读网格布局，正文和侧栏位置保持不变。
- CPU 与 NVIDIA GPU 两套 Docker Compose 配置解析通过。
- Android 离线工程确认不声明网络权限、使用 SQLite，内置 JSON 数据可解析。

## 发布包验证

- JAR：`knowledge-collector-boot/target/knowledge-collector.jar`
- 大小：85,144,819 字节
- 健康状态：UP
- Flyway：14
- 首页：HTTP 200
- 运维页：HTTP 200
- OpenAPI：HTTP 200
- AI Provider：1 个，本机 Ollama 可用
- Ollama 实机聊天：`deepseek-r1:14b` 成功，单次约 31.9 秒
- Compose CPU/GPU 配置：有效
- Android APK：本机未配置 Android SDK，未执行实际 APK 编译

## 结论

第一版本核心闭环及 Stage 10—27 通过自动化及发布包验收。系统已具备第三方能力管理，以及从搜索发现、采集、结构化分析、卡片证据、专题研究到综合写作和持续复习的本地知识生产链路；实际 Docker 镜像构建需先启动 Docker 服务，实际 APK 构建需安装 Android SDK。
