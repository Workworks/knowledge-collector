spring:
  datasource:
    url: jdbc:h2:mem:knowledge-collector-test;DB_CLOSE_DELAY=-1
  task:
    scheduling:
      enabled: false

knowledge-collector:
  security:
    enabled: false
  network:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
